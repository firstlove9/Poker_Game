# 注意: 请安装 python-socketio，不是 socketio！
# 错误的命令: pip install socketio
# 正确的命令: pip install -r requirements.txt
python-socketio>=5.0,<6.0
# macOS/Linux 需要 websocket-client 支持 WebSocket 传输
websocket-client