import { Card, PlayerAction } from '../types/poker';
import { GameState, RoomPlayer, WinnerInfo, PlayerHandInfo } from '../types/room';
export interface GameConfig {
    smallBlind: number;
    bigBlind: number;
    actionTimeout: number;
}
export interface SinglePlayerConfig {
    humanPlayerId: string;
    humanPlayerName: string;
    npcCount: number;
    buyIn: number;
}
export declare class SinglePlayerGameEngine {
    private state;
    private deck;
    private players;
    private config;
    private humanPlayerId;
    private npcTimeouts;
    private isRunning;
    private hasActedThisRound;
    private lastAggressorIndex;
    private actionCount;
    private playerInitialChips;
    constructor(config: SinglePlayerConfig, gameConfig: GameConfig);
    private createPlayers;
    getState(): GameState;
    getPlayers(): RoomPlayer[];
    getPlayerCards(playerId: string): [Card, Card] | undefined;
    isHumanPlayerTurn(): boolean;
    getPotAmount(): number;
    start(): void;
    private initPlayerRoles;
    private dealHoleCards;
    private postBlinds;
    private getNextActivePlayerIndex;
    private scheduleNpcActions;
    private decideNpcAction;
    executeAction(playerId: string, action: PlayerAction, amount?: number): boolean;
    private checkOnlyOnePlayerLeft;
    private isBettingRoundComplete;
    private advanceToNextPlayer;
    private advancePhase;
    private dealRemainingCommunityCards;
    private collectBets;
    private endHand;
    private determineWinners;
    getWinners(): WinnerInfo[];
    getAllHands(): PlayerHandInfo[];
    private getWinnersAndAllHands;
    rebuy(playerId: string, amount: number): void;
    nextHand(): void;
    cleanup(): void;
}
//# sourceMappingURL=SinglePlayerGameEngine.d.ts.map