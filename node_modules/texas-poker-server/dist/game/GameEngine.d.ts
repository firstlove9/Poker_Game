import { Card, PlayerAction } from '../types/poker';
import { GameState, RoomPlayer, WinnerInfo, PlayerHandInfo, PotResult } from '../types/room';
export interface GameConfig {
    smallBlind: number;
    bigBlind: number;
    actionTimeout: number;
}
export declare class GameEngine {
    private state;
    private deck;
    private players;
    private config;
    private hasActedThisRound;
    private lastAggressorIndex;
    private actionCount;
    private playerInitialChips;
    constructor(players: RoomPlayer[], dealerIndex: number, config: GameConfig);
    start(): GameState;
    private initPlayerRoles;
    private dealHoleCards;
    private postBlinds;
    private getNextActivePlayerIndex;
    performAction(playerId: string, action: PlayerAction, amount?: number): {
        success: boolean;
        error?: string;
    };
    private checkOnlyOnePlayerLeft;
    private isBettingRoundComplete;
    private advanceToNextPlayer;
    private advancePhase;
    private dealRemainingCommunityCards;
    private collectBets;
    private endHand;
    private determineWinners;
    showdown(): {
        winners: WinnerInfo[];
        potResults: PotResult[];
        allHands: PlayerHandInfo[];
    };
    getPotAmount(): number;
    private calcTotalPot;
    getState(): GameState;
    getPlayerCards(playerId: string): [Card, Card] | undefined;
    getPlayers(): RoomPlayer[];
    getCurrentPlayerId(): string | undefined;
    getValidActions(playerId: string): string[];
}
//# sourceMappingURL=GameEngine.d.ts.map